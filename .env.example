# Example .env file for Adsbot
# Copy this to `.env` and fill with your real secrets. Do NOT commit `.env` to git.

# Comma-separated API keys (PLACEHOLDERS)
# Replace these placeholders in your local `.env` with real keys; do NOT commit real keys.
ADMIN_API_KEYS=sk-admin-example-1,sk-admin-example-2
USER_API_KEYS=sk-user-example-1,sk-user-example-2

# Redis connection URL (optional). Example: redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# PayPal test credentials (PLACEHOLDERS)
# For local testing only — do not commit real credentials.
PAYPAL_USERNAME=you@example.com
PAYPAL_PASSWORD=change_me
